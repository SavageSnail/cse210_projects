using System.IO;
using System.Net;

public class Write
//the variables to be saved and added to the journal
{
    public string theCurrentTime;
    public string _gennedprompt;
    public string _userInput;


    // ALL THIS BELOW is just trial and error stuff
    /*
    //string dateText = theCurrentTime.ToShortDateString();
    public string _userEntry;
    public void record()
    {
        using (StreamWriter journalEntry = new StreamWriter(_userEntry))
        {
        // You can add text to the file with the WriteLine method
        journalEntry.WriteLine($"{theCurrentTime}- {_userInput}");
    
        // You can use the $ and include variables just like with Console.WriteLine
        //string color = "Blue";
        //outputFile.WriteLine($"My favorite color is {color}");
        }
    }
    */
}